import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  markerStyle: {
    width: 60,
    height: 30,
    backgroundColor: "#fff",
    borderRadius: 15,
    justifyContent: "center",
    alignItems: "center",
    borderWidth: 1,
    borderColor: "#000",
  },
  textStyle: {
    fontWeight: "bold",
    color: "#000",
  },
});
